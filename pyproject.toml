[project]
name = "ai-for-business-prognosis"
version = "0.1.0"
description = "Notebook environment for STL decomposition of the AirPassengers dataset."
requires-python = ">=3.11"
dependencies = [
    "ipykernel>=6.29.0",
    "matplotlib>=3.8.0",
    "pandas>=2.2.0",
    "statsmodels>=0.14.0",
]

[tool.uv]
package = false
